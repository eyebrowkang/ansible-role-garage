---
driver:
  name: vagrant
  provider:
    name: libvirt

platforms:
  - name: garage-multidisk
    box: generic/debian12
    cpus: 1
    memory: 1024

provisioner:
  name: ansible

verifier:
  name: ansible
