---
driver:
  name: vagrant
  provider:
    name: libvirt

platforms:
  - name: garage-negative
    box: generic/debian12
    cpus: 1
    memory: 1024

provisioner:
  name: ansible

verifier:
  name: ansible
