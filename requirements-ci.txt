# Pinned CI tooling — keep in sync with the local ansible-dev environment.
# Dependabot (pip ecosystem) opens PRs when new versions are released.
ansible-core==2.19.3
ansible-lint==25.12.1
molecule==26.4.0
molecule-plugins[docker]==25.8.12
yamllint==1.38.0
